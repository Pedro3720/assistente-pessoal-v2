# Onda 2.2 (Transferência entre contas) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Registrar transferências entre contas (saída de uma conta, entrada em outra) sem contar como receita/despesa do mês.

**Architecture:** Uma transferência = 2 lançamentos ligados por `transfer_group`, ambos `is_transfer=true` (saída expense da origem + entrada income no destino). Os saldos se ajustam pelo cálculo existente; os totais de Receitas/Despesas e a quebra por categoria passam a excluir `is_transfer`. UI: checkbox "É transferência" no modal de despesa + "Conta destino".

**Tech Stack:** Next.js 16.2.9, React 19, TS strict, Supabase, Zod, `node:crypto` (`randomUUID`).

## Global Constraints

- **Projeto alvo:** `C:\Projetos\assistente-pessoal-v2` (Git Bash: `/c/Projetos/assistente-pessoal-v2`). O shell dos subagentes abre em OUTRA pasta — usar caminhos absolutos e `cd /c/Projetos/assistente-pessoal-v2 && <cmd>` sempre.
- **Branch de trabalho:** `feat/onda2-transferencia`. Commitar nela.
- **NÃO tocar** em `.env.local` nem `CONTEXT.md`. **TS strict, sem `any`.**
- **No test framework** — gate por task = **`npm run build` passa** + verificação. Commits frequentes.
- **Migrações aplicadas MANUALMENTE** no SQL Editor (entregar SQL; não rodar SQL na task).
- **Invariante:** transferência **não** conta como receita nem despesa (nem na quebra por categoria); os saldos das contas se ajustam automaticamente (o cálculo de saldo já soma todas as transações por conta).
- **Padrão de mutação:** Server Action ("use server", Zod, `user_id` via `ctx()`/`auth.getUser()`, `revalidate()`).

## Setup (uma vez)

```bash
cd /c/Projetos/assistente-pessoal-v2
git checkout main && git pull --ff-only
git checkout -b feat/onda2-transferencia
```

## File Structure

**Criar:** `supabase/migrations/20260701000009_tx_transfer.sql`.
**Modificar:** `src/types/finance.ts`, `src/lib/validation/finance.ts`, `src/lib/actions/finance.ts` (Task 1);
`src/lib/data/finance.ts`, `src/app/(app)/financas/page.tsx` (Task 2);
`src/components/finance/transactions-section.tsx` (Task 3).

---

### Task 1: Backend da transferência (migração + tipo + validação + actions)

**Files:**
- Create: `supabase/migrations/20260701000009_tx_transfer.sql`
- Modify: `src/types/finance.ts`, `src/lib/validation/finance.ts`, `src/lib/actions/finance.ts`

**Interfaces:**
- Consumes: `ctx()`, `revalidate()`, `randomUUID` (já importado em `actions/finance.ts`).
- Produces: `Transaction` ganha `is_transfer`/`transfer_group`; `transferInput` (Zod); `createTransfer(raw)`; `deleteTransferGroup(group: string)`.

- [ ] **Step 1: Migração `20260701000009_tx_transfer.sql`**

```sql
-- Migração 0009: transferência entre contas
alter table public.transactions add column if not exists is_transfer boolean not null default false;
alter table public.transactions add column if not exists transfer_group uuid;
create index if not exists tx_transfer_group_idx on public.transactions (transfer_group);
notify pgrst, 'reload schema';
```

- [ ] **Step 2: Tipo em `src/types/finance.ts`**

No `interface Transaction`, adicionar ao final dos campos:

```ts
  is_transfer: boolean;
  transfer_group: string | null;
```

- [ ] **Step 3: `transferInput` em `src/lib/validation/finance.ts`**

Adicionar ao final:

```ts
export const transferInput = z
  .object({
    description: z.string().trim().min(1, "Descrição obrigatória"),
    amount: z.number().positive("O valor deve ser maior que zero"),
    from_bank_id: z.number().int(),
    to_bank_id: z.number().int(),
    occurred_on: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, "Data inválida"),
  })
  .refine((v) => v.from_bank_id !== v.to_bank_id, {
    message: "Escolha contas diferentes",
    path: ["to_bank_id"],
  });
export type TransferInput = z.infer<typeof transferInput>;
```

- [ ] **Step 4: Actions em `src/lib/actions/finance.ts`**

Adicionar `transferInput` ao import de `@/lib/validation/finance` (que já traz `transactionInput`, `installmentInput`, etc.). E adicionar as actions (perto das outras de transação):

```ts
export async function createTransfer(raw: unknown) {
  const input = transferInput.parse(raw);
  const { supabase, userId } = await ctx();
  const group = randomUUID();
  const base = {
    description: input.description,
    amount: input.amount,
    category_id: null,
    card_id: null,
    is_card_payment: false,
    occurred_on: input.occurred_on,
    is_transfer: true,
    transfer_group: group,
    user_id: userId,
  };
  const rows = [
    { ...base, type: "expense" as const, bank_id: input.from_bank_id },
    { ...base, type: "income" as const, bank_id: input.to_bank_id },
  ];
  const { error } = await supabase.from("transactions").insert(rows);
  if (error) throw new Error(error.message);
  revalidate();
}

export async function deleteTransferGroup(group: string) {
  const { supabase } = await ctx();
  const { error } = await supabase.from("transactions").delete().eq("transfer_group", group);
  if (error) throw new Error(error.message);
  revalidate();
}
```

- [ ] **Step 5: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro. (A migração não é aplicada; o build só checa tipos. `select("*")` no mês traz as novas colunas em runtime; o tipo `Transaction` agora as inclui.)

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260701000009_tx_transfer.sql src/types/finance.ts src/lib/validation/finance.ts src/lib/actions/finance.ts
git commit -m "feat(financas): backend de transferencia entre contas (#8)"
```

> **Passo manual do usuário:** rodar `20260701000009_tx_transfer.sql` no SQL Editor.

---

### Task 2: Excluir transferências dos totais e da quebra por categoria

**Files:**
- Modify: `src/lib/data/finance.ts`
- Modify: `src/app/(app)/financas/page.tsx`

**Interfaces:**
- Consumes: `Transaction.is_transfer` (Task 1).

- [ ] **Step 1: Totais em `src/lib/data/finance.ts`**

Trocar o cálculo de `income`/`expense`:

```ts
  const income = monthTransactions
    .filter((t) => t.type === "income")
    .reduce((s, t) => s + num(t.amount), 0);
```

por (excluir transferências):

```ts
  const income = monthTransactions
    .filter((t) => t.type === "income" && !t.is_transfer)
    .reduce((s, t) => s + num(t.amount), 0);
```

E trocar:

```ts
  const expense = monthTransactions
    .filter((t) => t.type === "expense" && !t.is_card_payment)
    .reduce((s, t) => s + num(t.amount), 0);
```

por:

```ts
  const expense = monthTransactions
    .filter((t) => t.type === "expense" && !t.is_card_payment && !t.is_transfer)
    .reduce((s, t) => s + num(t.amount), 0);
```

- [ ] **Step 2: Quebra por categoria em `src/app/(app)/financas/page.tsx`**

No loop `byCat`, trocar a linha de `continue`:

```tsx
    if (t.type !== "expense" || t.is_card_payment) continue;
```

por:

```tsx
    if (t.type !== "expense" || t.is_card_payment || t.is_transfer) continue;
```

- [ ] **Step 3: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro.

- [ ] **Step 4: Commit**

```bash
git add src/lib/data/finance.ts src/app/\(app\)/financas/page.tsx
git commit -m "feat(financas): transferencias fora de receitas/despesas e categorias (#8)"
```

---

### Task 3: UI da transferência (`transactions-section.tsx`)

**Files:**
- Modify: `src/components/finance/transactions-section.tsx`

**Interfaces:**
- Consumes: `createTransfer`, `deleteTransferGroup` (Task 1); `Transaction.is_transfer`/`transfer_group`.

- [ ] **Step 1: Imports das actions**

Na importação de `@/lib/actions/finance`, acrescentar `createTransfer` e `deleteTransferGroup`:

```tsx
import {
  createTransaction, updateTransaction, deleteTransaction,
  ensureDefaultCategories, createInstallmentPurchase, deleteTransactionGroup,
  createTransfer, deleteTransferGroup,
} from "@/lib/actions/finance";
```

- [ ] **Step 2: Estados `isTransfer`/`toBankId` + reset**

Junto aos outros `useState` do modal, adicionar:

```tsx
  const [isTransfer, setIsTransfer] = useState(false);
  const [toBankId, setToBankId] = useState("");
```

Em `reset()`, adicionar ao final:

```tsx
    setIsTransfer(false);
    setToBankId("");
```

- [ ] **Step 3: `openEdit` bloqueia edição de transferência**

No início de `openEdit(t)`, antes de qualquer `setX`, adicionar:

```tsx
    if (t.transfer_group) {
      toast.info("Para alterar uma transferência, exclua e recrie.");
      return;
    }
```

- [ ] **Step 4: `save()` roteia transferência**

Substituir o bloco `try { ... }` interno do `save()` (o trecho `if (editingId) {...} else if (isPurchase ...) {...} else {...}`) por:

```tsx
    try {
      if (editingId) {
        await updateTransaction(editingId, input);
      } else if (isTransfer) {
        if (!bankId || !toBankId) {
          toast.error("Escolha a conta de origem e a de destino.");
          setSaving(false);
          return;
        }
        await createTransfer({
          description: description.trim(),
          amount: value,
          from_bank_id: Number(bankId),
          to_bank_id: Number(toBankId),
          occurred_on: date,
        });
      } else if (isPurchase && Number(parcelas) > 1) {
        await createInstallmentPurchase({ ...input, installments: Number(parcelas) });
      } else {
        await createTransaction(input);
      }
      setOpen(false);
      reset();
      router.refresh();
    } catch (e) {
      toast.error(e instanceof Error ? e.message : "Erro ao salvar");
    } finally {
      setSaving(false);
    }
```

- [ ] **Step 5: `remove()` trata `transfer_group`**

Substituir a função `remove` por:

```tsx
  async function remove(id: number) {
    const t = transactions.find((x) => x.id === id);
    try {
      if (t?.transfer_group) {
        if (!confirm("Excluir a transferência (os dois lançamentos)?")) return;
        await deleteTransferGroup(t.transfer_group);
      } else if (t?.purchase_group) {
        if (!confirm("Excluir a compra parcelada inteira (todas as parcelas)?")) return;
        await deleteTransactionGroup(t.purchase_group);
      } else {
        await deleteTransaction(id);
      }
      router.refresh();
    } catch (e) {
      toast.error(e instanceof Error ? e.message : "Erro ao excluir");
    }
  }
```

- [ ] **Step 6: Checkbox "É transferência" + campos condicionais no modal**

No modal, trocar o bloco do checkbox "É pagamento de fatura de cartão" (o `{type === "expense" && (<label ...>...</label>)}`) por dois checkboxes mutuamente exclusivos:

```tsx
            {type === "expense" && (
              <div className="grid grid-cols-1 gap-2 sm:grid-cols-2">
                <label className="flex items-center gap-2 rounded-lg border border-border bg-muted/50 p-3 text-sm">
                  <input
                    type="checkbox"
                    checked={isCardPayment}
                    onChange={(e) => {
                      setIsCardPayment(e.target.checked);
                      if (e.target.checked) {
                        setCategoryId("");
                        setIsTransfer(false);
                        setToBankId("");
                      }
                    }}
                    className="accent-primary"
                  />
                  Pagamento de fatura
                </label>
                <label className="flex items-center gap-2 rounded-lg border border-border bg-muted/50 p-3 text-sm">
                  <input
                    type="checkbox"
                    checked={isTransfer}
                    onChange={(e) => {
                      setIsTransfer(e.target.checked);
                      if (e.target.checked) {
                        setIsCardPayment(false);
                        setCategoryId("");
                        setCardId("");
                      } else {
                        setToBankId("");
                      }
                    }}
                    className="accent-primary"
                  />
                  Transferência entre contas
                </label>
              </div>
            )}
```

Trocar a condição do bloco de **categoria** (`{!isCardPayment && (`) por:

```tsx
            {!isCardPayment && !isTransfer && (
```

Trocar a condição do bloco de **cartão** (`{type === "expense" && cards.length > 0 && (`) por:

```tsx
            {type === "expense" && cards.length > 0 && !isTransfer && (
```

No bloco de **Conta**, trocar o `<label>` e adicionar a conta destino logo depois. Substituir:

```tsx
            <div className="space-y-1">
              <label className="text-sm font-medium">Conta</label>
              <select
                value={isPurchase ? "" : bankId}
                disabled={isPurchase}
                onChange={(e) => setBankId(e.target.value)}
                className="w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm disabled:opacity-50"
              >
                <option value="">{isPurchase ? "— compra vai para a fatura —" : "Sem conta"}</option>
                {banks.map((b) => (
                  <option key={b.id} value={b.id}>
                    {b.icon} {b.name}
                  </option>
                ))}
              </select>
            </div>
```

por:

```tsx
            <div className="space-y-1">
              <label className="text-sm font-medium">{isTransfer ? "Conta origem" : "Conta"}</label>
              <select
                value={isPurchase ? "" : bankId}
                disabled={isPurchase}
                onChange={(e) => setBankId(e.target.value)}
                className="w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm disabled:opacity-50"
              >
                <option value="">{isPurchase ? "— compra vai para a fatura —" : "Sem conta"}</option>
                {banks.map((b) => (
                  <option key={b.id} value={b.id}>
                    {b.icon} {b.name}
                  </option>
                ))}
              </select>
            </div>

            {isTransfer && (
              <div className="space-y-1">
                <label className="text-sm font-medium">Conta destino</label>
                <select
                  value={toBankId}
                  onChange={(e) => setToBankId(e.target.value)}
                  className="w-full rounded-lg border border-border bg-muted px-3 py-2 text-sm"
                >
                  <option value="">Selecione a conta destino</option>
                  {banks
                    .filter((b) => String(b.id) !== bankId)
                    .map((b) => (
                      <option key={b.id} value={b.id}>
                        {b.icon} {b.name}
                      </option>
                    ))}
                </select>
              </div>
            )}
```

- [ ] **Step 7: `TxRow` marca transferência**

No componente `TxRow`, trocar a linha de metadados (o `<p className="text-xs text-muted-foreground">...`) por:

```tsx
          <p className="text-xs text-muted-foreground">
            {t.is_transfer ? "🔄 transferência" : cat ? `${cat.icon} ${cat.name}` : "Sem categoria"}
            {t.is_card_payment ? " · 💳 pagamento" : ""} · {formatDateBR(t.occurred_on)}
          </p>
```

- [ ] **Step 8: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro.

- [ ] **Step 9: Verificação manual (com a migração 0009 aplicada)**

`/financas` → Nova → Despesa → escolher **Conta** → marcar **"Transferência entre contas"** → some categoria/cartão, "Conta" vira "Conta origem" e aparece "Conta destino" → escolher destino → valor → Salvar.
Esperado: saldo da origem cai, saldo do destino sobe pelo mesmo valor; **Receitas/Despesas do mês não mudam**; os 2 lançamentos aparecem com **"🔄 transferência"**; excluir um pede confirmação e remove os dois.

- [ ] **Step 10: Commit**

```bash
git add src/components/finance/transactions-section.tsx
git commit -m "feat(financas): UI de transferencia entre contas (#8)"
```

---

### Task 4: Verificação final

- [ ] **Step 1: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: sem erros.

- [ ] **Step 2: Integrar o branch**

Usar `superpowers:finishing-a-development-branch` para merge/push de `feat/onda2-transferencia`.

## Self-Review (autor do plano)

**Cobertura do spec:**
- Migração + tipo + validação + `createTransfer`/`deleteTransferGroup` → Task 1. ✅
- Excluir `is_transfer` de totais + categoria → Task 2. ✅
- UI (checkbox, conta destino, save, TxRow, remove, bloqueio de edição) → Task 3. ✅
- Passo manual (migração 0009) → nota na Task 1. ✅

**Consistência de tipos/nomes:** `is_transfer`/`transfer_group`, `transferInput`, `createTransfer({description,amount,from_bank_id,to_bank_id,occurred_on})`, `deleteTransferGroup`, estados `isTransfer`/`toBankId` — usados igual entre as tasks. ✅

**Placeholders:** nenhum "TBD/TODO"; código completo em cada passo. ✅
