# PWA Fase 2 (offline + service worker unificado) Implementation Plan

> Implementar tarefa a tarefa. Sem framework de testes: validação por `npm run build` +
> verificação no navegador (registro do SW, cache, página offline). Teste real de "sem conexão"
> é no site publicado / iPhone do dono.

**Goal:** o app carregar mais rápido (cache de estáticos) e mostrar uma página offline amigável
sem conexão, sem quebrar o push que já funciona.

**Architecture:** estender `public/sw.js` na mão (Cache API) mantendo os handlers de push. Registrar
o SW no carregamento para todos (hoje só registra quem ativa push). Página offline estática em `public`.

**Tech Stack:** Service Worker (Cache API), Next 16, React.

## Global Constraints

- **Nunca usar travessão `—`/`–` em texto visível** (regra do `CLAUDE.md`).
- **Não quebrar o push:** preservar os handlers `push` e `notificationclick` do `sw.js` atual.
- Cor de marca: `#080b12`. Validação por `npm run build` + navegador.
- Não cachear: cross-origin (Supabase/Google), `/api/*`, respostas autenticadas de navegação.

---

### Task 1: Página offline estática

**Files:**
- Create: `public/offline.html`

**Interfaces:**
- Produces: `/offline.html`, servido pelo SW quando uma navegação falha sem conexão (Task 2).

- [ ] **Step 1: criar** `public/offline.html`

```html
<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<title>Sem conexão</title>
<style>
  html, body { margin: 0; height: 100%; }
  body {
    background: #080b12; color: #e9edf5;
    font-family: system-ui, -apple-system, sans-serif;
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    text-align: center; padding: 24px; gap: 16px;
  }
  img { width: 72px; height: 72px; opacity: 0.9; }
  h1 { font-size: 20px; font-weight: 700; margin: 0; }
  p { color: #94a3b8; font-size: 14px; max-width: 320px; margin: 0; line-height: 1.5; }
  button {
    margin-top: 8px; background: #3b82f6; color: #fff; border: 0;
    border-radius: 12px; padding: 12px 20px; font-size: 14px; font-weight: 600;
  }
</style>
</head>
<body>
  <img src="/icons/icon-192.png" alt="Zênite" />
  <h1>Você está sem conexão</h1>
  <p>Não foi possível carregar o Zênite agora. Verifique sua internet e tente de novo.</p>
  <button onclick="location.reload()">Tentar de novo</button>
</body>
</html>
```

- [ ] **Step 2: verificar** no dev server: navegar para `/offline.html` e confirmar que renderiza
  (fundo escuro, ícone, botão).

- [ ] **Step 3: commit**

```bash
git add public/offline.html
git commit -m "feat(pwa): pagina offline estatica"
```

---

### Task 2: Estender o service worker (cache + offline), mantendo push

**Files:**
- Modify: `public/sw.js` (reescrever com cache + os handlers de push atuais preservados)

**Interfaces:**
- Consumes: `/offline.html` (Task 1).
- Produces: SW que cacheia estáticos e serve `/offline.html` em navegação sem rede; mantém push.

- [ ] **Step 1: reescrever** `public/sw.js`

```js
/* Service worker do Zênite: push (Onda 7) + cache/offline (PWA Fase 2). */
const VERSION = "v1";
const CACHE = `zenite-${VERSION}`;
const PRECACHE = ["/offline.html", "/icons/icon-192.png", "/manifest.webmanifest"];

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE).then((c) => c.addAll(PRECACHE)).then(() => self.skipWaiting())
  );
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    caches
      .keys()
      .then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

function isStaticAsset(url) {
  return (
    url.pathname.startsWith("/_next/static/") ||
    url.pathname.startsWith("/icons/") ||
    url.pathname === "/logo.png" ||
    url.pathname === "/manifest.webmanifest"
  );
}

self.addEventListener("fetch", (event) => {
  const req = event.request;
  if (req.method !== "GET") return;
  const url = new URL(req.url);
  if (url.origin !== self.location.origin) return; // cross-origin (Supabase/Google) vai direto pra rede
  if (url.pathname.startsWith("/api/") || url.pathname.includes("hot-update")) return; // nunca cachear

  // Navegações: rede primeiro; sem conexão, cai na página offline
  if (req.mode === "navigate") {
    event.respondWith(fetch(req).catch(() => caches.match("/offline.html")));
    return;
  }

  // Assets estáticos (hasheados): stale-while-revalidate
  if (isStaticAsset(url)) {
    event.respondWith(
      caches.open(CACHE).then(async (cache) => {
        const cached = await cache.match(req);
        const network = fetch(req)
          .then((res) => {
            if (res && res.status === 200) cache.put(req, res.clone());
            return res;
          })
          .catch(() => cached);
        return cached || network;
      })
    );
  }
});

/* ── Push (mantido da Onda 7) ─────────────────────────── */
self.addEventListener("push", (event) => {
  let data = {};
  try {
    data = event.data ? event.data.json() : {};
  } catch (e) {
    data = {};
  }
  const title = data.title || "Zênite";
  event.waitUntil(
    self.registration.showNotification(title, {
      body: data.body || "",
      icon: "/logo.png",
      badge: "/logo.png",
      tag: data.tag,
      data: { url: data.url || "/" },
    })
  );
});

self.addEventListener("notificationclick", (event) => {
  event.notification.close();
  const url = (event.notification.data && event.notification.data.url) || "/";
  event.waitUntil(
    self.clients.matchAll({ type: "window", includeUncontrolled: true }).then((wins) => {
      const hit = wins.find((w) => w.url.endsWith(url));
      if (hit) return hit.focus();
      return self.clients.openWindow(url);
    })
  );
});
```

- [ ] **Step 2: verificar** no dev server: registrar o SW (Task 3 faz isso) e confirmar via JS que
  `caches` contém `zenite-v1` com `/offline.html` pré-cacheado.

- [ ] **Step 3: commit**

```bash
git add public/sw.js
git commit -m "feat(pwa): sw com cache de estaticos + fallback offline (mantem push)"
```

---

### Task 3: Registrar o SW no carregamento (para todos)

**Files:**
- Create: `src/components/pwa/register-sw.tsx`
- Modify: `src/app/layout.tsx` (renderizar `<RegisterSW />` no `<body>`)

**Interfaces:**
- Produces: registro de `/sw.js` em toda carga de página (não só ao ativar push). O `subscribeToPush`
  continua funcionando (o `register` é idempotente).

- [ ] **Step 1: criar** `src/components/pwa/register-sw.tsx`

```tsx
"use client";

import { useEffect } from "react";

export function RegisterSW() {
  useEffect(() => {
    if (!("serviceWorker" in navigator)) return;
    const register = () => {
      navigator.serviceWorker.register("/sw.js").catch(() => {});
    };
    if (document.readyState === "complete") register();
    else window.addEventListener("load", register, { once: true });
  }, []);
  return null;
}
```

- [ ] **Step 2: renderizar** em `src/app/layout.tsx` (import + dentro do `<body>`, junto dos outros)

```tsx
import { RegisterSW } from "@/components/pwa/register-sw";
```

Dentro do `<body>`, antes do `</ThemeProvider>` fechar (ao lado de `ThemedToaster`):

```tsx
          <RegisterSW />
```

- [ ] **Step 3: build**

Run: `npm run build`
Expected: compila sem erro.

- [ ] **Step 4: verificar** no dev server: recarregar, confirmar via JS que
  `navigator.serviceWorker.controller` fica definido e `caches.keys()` inclui `zenite-v1`.

- [ ] **Step 5: commit**

```bash
git add src/app/layout.tsx src/components/pwa/register-sw.tsx
git commit -m "feat(pwa): registra o service worker em toda carga (cache/offline pra todos)"
```

---

## Self-Review (feito)

- **Cobertura do spec (Fase 2):** SW unificado push+cache (Task 2), precache/offline (Task 1),
  registro universal (Task 3). Coberto.
- **Não quebra push:** handlers `push`/`notificationclick` preservados verbatim na Task 2.
- **Placeholders:** nenhum; código completo.
- **Riscos:** SW novo substitui o antigo via `skipWaiting`+`clients.claim`; inscrições de push
  persistem (ligadas ao registration, não ao script). Teste real de offline é no site publicado.
