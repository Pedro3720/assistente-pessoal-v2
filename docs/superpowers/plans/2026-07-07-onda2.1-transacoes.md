# Onda 2.1 (Transações) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir o "não registra" (#6a) fazendo a nova transação nascer no mês visualizado, e adicionar um modal em tela cheia "Ver todas" com a lista completa editável (#6b).

**Architecture:** O `Modal` ganha um tamanho `full`; a página de Finanças passa `defaultDate` (mês visualizado) e `monthLabel` ao `TransactionsSection`, que usa `defaultDate` na nova transação, extrai a linha para um `TxRow` reutilizável, e adiciona um botão "Ver todas" que abre a lista completa num `Modal size="full"`.

**Tech Stack:** Next.js 16.2.9 (App Router), React 19, TS strict, Tailwind v4, `next/*`.

## Global Constraints

- **Projeto alvo:** `C:\Projetos\assistente-pessoal-v2` (Git Bash: `/c/Projetos/assistente-pessoal-v2`). O shell dos subagentes abre em OUTRA pasta — usar caminhos absolutos e `cd /c/Projetos/assistente-pessoal-v2 && <cmd>` sempre.
- **Branch de trabalho:** `feat/onda2-transacoes`. Commitar nela.
- **NÃO tocar** em `.env.local` nem `CONTEXT.md`. **TS strict, sem `any`.**
- **No test framework** — gate por task = **`npm run build` passa** + verificação manual. Commits frequentes.
- **Mutação client:** Server Action + `router.refresh()` + `toast` em erro (padrão já existente no arquivo — reusar, não reescrever a lógica de salvar/editar/excluir).
- **UI pt-BR.** Camada visual: `glass card-glow`, `.num`, variantes `dark:`.
- **Z-index dos modais:** o modal de edição/criação (`Modal size="md"`) deve ser renderizado **depois** do modal de tela cheia no JSX, para ficar por cima (mesmo `z-[100]`, quem entra depois no portal vence).

## Setup (uma vez)

```bash
cd /c/Projetos/assistente-pessoal-v2
git checkout main && git pull --ff-only
git checkout -b feat/onda2-transacoes
```

## File Structure

**Modificar:**
- `src/components/ui/modal.tsx` — prop `size?: "md" | "full"`.
- `src/components/finance/transactions-section.tsx` — props novas, `defaultDate` (#6a), `TxRow`, "Ver todas" (#6b).
- `src/app/(app)/financas/page.tsx` — passar `defaultDate` + `monthLabel`.

---

### Task 1: `Modal` com tamanho (`size`)

**Files:**
- Modify: `src/components/ui/modal.tsx`

**Interfaces:**
- Produces: `Modal({ onClose, title, children, size })` — `size?: "md" | "full"` (default `"md"`).

- [ ] **Step 1: Adicionar a prop `size` e aplicar a largura**

Substituir a assinatura e o container interno. Trocar:

```tsx
export function Modal({
  onClose,
  title,
  children,
}: {
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
}) {
```

por:

```tsx
export function Modal({
  onClose,
  title,
  children,
  size = "md",
}: {
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  size?: "md" | "full";
}) {
```

E trocar o `<div>` do container interno:

```tsx
      <div
        className="max-h-[90vh] w-full max-w-md overflow-y-auto rounded-2xl border border-border bg-popover p-6 shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
```

por:

```tsx
      <div
        className={`overflow-y-auto rounded-2xl border border-border bg-popover p-6 shadow-xl ${
          size === "full" ? "max-h-[92vh] w-[95vw] max-w-4xl" : "max-h-[90vh] w-full max-w-md"
        }`}
        onClick={(e) => e.stopPropagation()}
      >
```

- [ ] **Step 2: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro (a prop é opcional; usos atuais do `Modal` seguem válidos).

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/modal.tsx
git commit -m "feat(ui): Modal com tamanho md/full"
```

---

### Task 2: Transações — data no mês visualizado (#6a) + tela cheia (#6b)

**Files:**
- Modify: `src/components/finance/transactions-section.tsx`

**Interfaces:**
- Consumes: `Modal` (aceita `size`, Task 1).
- Produces: `TransactionsSection` passa a exigir props **`defaultDate: string`** e **`monthLabel: string`** (além das atuais). (A `financas/page.tsx`, Task 3, passa essas props.)

- [ ] **Step 1: Props novas + data padrão (#6a)**

Trocar a assinatura do componente:

```tsx
export function TransactionsSection({
  transactions,
  categories,
  banks,
  cards,
}: {
  transactions: Transaction[];
  categories: Category[];
  banks: BankWithBalance[];
  cards: CardWithInvoice[];
}) {
```

por:

```tsx
export function TransactionsSection({
  transactions,
  categories,
  banks,
  cards,
  defaultDate,
  monthLabel,
}: {
  transactions: Transaction[];
  categories: Category[];
  banks: BankWithBalance[];
  cards: CardWithInvoice[];
  defaultDate: string;
  monthLabel: string;
}) {
```

Trocar a inicialização do estado `date`:

```tsx
  const [date, setDate] = useState(todayISO());
```

por:

```tsx
  const [date, setDate] = useState(defaultDate);
```

Trocar, dentro de `reset()`:

```tsx
    setDate(todayISO());
```

por:

```tsx
    setDate(defaultDate);
```

> `todayISO` continua importado (usado indiretamente? não). Se após a troca `todayISO` ficar sem uso no arquivo, remover do import de `@/lib/dates` (deixando `formatDateBR`). Conferir com o build.

- [ ] **Step 2: Adicionar estado do modal de tela cheia**

Logo após `const [saving, setSaving] = useState(false);`, adicionar:

```tsx
  const [fullOpen, setFullOpen] = useState(false);
```

- [ ] **Step 3: Extrair `TxRow` (DRY) e os filtros**

No fim do arquivo (fora do componente `TransactionsSection`), adicionar o componente de linha:

```tsx
function TxRow({
  t,
  cat,
  onEdit,
  onRemove,
}: {
  t: Transaction;
  cat: Category | null;
  onEdit: () => void;
  onRemove: () => void;
}) {
  return (
    <div className="flex items-center justify-between rounded-lg p-3 transition-colors hover:bg-accent/30">
      <div className="flex min-w-0 items-center gap-3">
        <div className={`shrink-0 rounded-full p-2 ${t.type === "income" ? "bg-green-100" : "bg-red-100"}`}>
          {t.type === "income" ? (
            <ArrowUpRight className="h-4 w-4 text-green-600" />
          ) : (
            <ArrowDownRight className="h-4 w-4 text-red-600" />
          )}
        </div>
        <div className="min-w-0">
          <p className="truncate font-medium">{t.description}</p>
          <p className="text-xs text-muted-foreground">
            {cat ? `${cat.icon} ${cat.name}` : "Sem categoria"}
            {t.is_card_payment ? " · 💳 pagamento" : ""} · {formatDateBR(t.occurred_on)}
          </p>
        </div>
      </div>
      <div className="flex shrink-0 items-center gap-2">
        <span className={`num font-semibold ${t.type === "income" ? "text-green-600 dark:text-green-400" : "text-red-600 dark:text-red-400"}`}>
          {t.type === "income" ? "+" : "-"}
          {formatBRL(Number(t.amount))}
        </span>
        <button onClick={onEdit} className="rounded-lg p-2 text-muted-foreground hover:bg-accent">
          <Edit3 className="h-4 w-4" />
        </button>
        <button onClick={onRemove} className="rounded-lg p-2 text-muted-foreground hover:bg-accent">
          <Trash2 className="h-4 w-4" />
        </button>
      </div>
    </div>
  );
}
```

Dentro de `TransactionsSection`, logo antes do `return (`, definir os filtros reutilizáveis (usados no cabeçalho e na tela cheia):

```tsx
  const filterTabs = (["all", "income", "expense"] as const).map((f) => (
    <button
      key={f}
      onClick={() => setFilter(f)}
      className={`rounded-full px-3 py-1 text-xs font-medium transition-colors ${
        filter === f
          ? "bg-primary text-primary-foreground"
          : "bg-accent text-accent-foreground hover:bg-accent/80"
      }`}
    >
      {f === "all" ? "Todas" : f === "income" ? "Receitas" : "Despesas"}
    </button>
  ));
```

- [ ] **Step 4: Cabeçalho usa `filterTabs` + botão "Ver todas"**

Trocar o bloco do cabeçalho (o `<div className="flex flex-col gap-4 ...">` inteiro) por:

```tsx
      <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
        <h3 className="font-semibold">Transações</h3>
        <div className="flex flex-wrap items-center gap-2">
          {filterTabs}
          <button
            onClick={() => setFullOpen(true)}
            className="rounded-lg border border-border px-3 py-1.5 text-xs font-medium text-muted-foreground hover:bg-accent"
          >
            Ver todas
          </button>
          <button
            onClick={openNew}
            className="flex items-center gap-1 rounded-lg bg-primary px-3 py-1.5 text-xs font-medium text-primary-foreground hover:bg-primary/90"
          >
            <Plus className="h-3.5 w-3.5" /> Nova
          </button>
        </div>
      </div>
```

- [ ] **Step 5: Card usa `TxRow`**

Trocar o corpo da lista do card (o `<div className="mt-4 space-y-2">...</div>`) por:

```tsx
      <div className="mt-4 space-y-2">
        {shown.length === 0 ? (
          <p className="py-6 text-center text-sm text-muted-foreground">
            Nenhuma transação neste mês.
          </p>
        ) : (
          shown.map((t) => (
            <TxRow
              key={t.id}
              t={t}
              cat={t.category_id ? catById.get(t.category_id) ?? null : null}
              onEdit={() => openEdit(t)}
              onRemove={() => remove(t.id)}
            />
          ))
        )}
      </div>
```

- [ ] **Step 6: Modal de tela cheia "Ver todas" (#6b)**

Inserir este bloco **antes** do `{open && (<Modal ...>` existente (para o modal de edição ficar renderizado depois → por cima):

```tsx
      {fullOpen && (
        <Modal size="full" title={`Transações — ${monthLabel}`} onClose={() => setFullOpen(false)}>
          <div className="mb-4 flex flex-wrap items-center gap-2">
            {filterTabs}
            <button
              onClick={openNew}
              className="flex items-center gap-1 rounded-lg bg-primary px-3 py-1.5 text-xs font-medium text-primary-foreground hover:bg-primary/90"
            >
              <Plus className="h-3.5 w-3.5" /> Nova
            </button>
          </div>
          <div className="space-y-2">
            {shown.length === 0 ? (
              <p className="py-8 text-center text-sm text-muted-foreground">Nenhuma transação neste mês.</p>
            ) : (
              shown.map((t) => (
                <TxRow
                  key={t.id}
                  t={t}
                  cat={t.category_id ? catById.get(t.category_id) ?? null : null}
                  onEdit={() => openEdit(t)}
                  onRemove={() => remove(t.id)}
                />
              ))
            )}
          </div>
        </Modal>
      )}
```

- [ ] **Step 7: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro. (Se `todayISO` ficou sem uso, remover do import — o build de TS strict acusa import não usado? O Next não falha por import não usado por padrão, mas remova para ficar limpo.)

- [ ] **Step 8: Commit**

```bash
git add src/components/finance/transactions-section.tsx
git commit -m "feat(financas): data no mes visualizado (#6a) e tela cheia de transacoes (#6b)"
```

---

### Task 3: `financas/page.tsx` passa `defaultDate` + `monthLabel`

**Files:**
- Modify: `src/app/(app)/financas/page.tsx`

**Interfaces:**
- Consumes: `TransactionsSection` agora exige `defaultDate: string` e `monthLabel: string` (Task 2).

- [ ] **Step 1: Importar `todayISO` e calcular `defaultDate`**

No import de `@/lib/dates` (que já traz `currentYearMonth, shiftMonth, monthLabel`), acrescentar `todayISO`:

```tsx
import { currentYearMonth, shiftMonth, monthLabel, todayISO } from "@/lib/dates";
```

Dentro do componente, após `const { year, month } = shiftMonth(cy, cm, offset);` (as variáveis `cy`/`cm` já vêm de `currentYearMonth()` na linha acima), adicionar:

```tsx
  const defaultDate =
    year === cy && month === cm
      ? todayISO()
      : `${year}-${String(month).padStart(2, "0")}-01`;
```

- [ ] **Step 2: Passar as props ao `TransactionsSection`**

Trocar o uso do componente:

```tsx
          <TransactionsSection
            transactions={monthTransactions}
            categories={categories}
            banks={banks}
            cards={cards}
          />
```

por:

```tsx
          <TransactionsSection
            transactions={monthTransactions}
            categories={categories}
            banks={banks}
            cards={cards}
            defaultDate={defaultDate}
            monthLabel={monthLabel(year, month)}
          />
```

- [ ] **Step 3: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: compila sem erro (props exigidas agora satisfeitas).

- [ ] **Step 4: Verificação manual**

`npm run dev` → `/financas`:
- Navegar para um mês passado (setas do `MonthNav`) → clicar **Nova** → o campo **Data já vem naquele mês** (dia 1); salvar → a transação aparece no mês aberto.
- No mês atual, **Nova** vem com a data de hoje.
- Botão **"Ver todas"** abre a tela cheia com a lista completa do mês; **editar** por lá abre o modal de edição **por cima** e salva; **excluir** funciona (e pede confirmação em compra parcelada).

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/financas/page.tsx
git commit -m "feat(financas): passar defaultDate e monthLabel para as transacoes"
```

---

### Task 4: Verificação final

- [ ] **Step 1: Build**

Run: `cd /c/Projetos/assistente-pessoal-v2 && npm run build`
Expected: sem erros.

- [ ] **Step 2: Integrar o branch**

Usar `superpowers:finishing-a-development-branch` para merge/push de `feat/onda2-transacoes`.

## Self-Review (autor do plano)

**Cobertura do spec:**
- `Modal size` → Task 1. ✅
- #6a data no mês visualizado → Task 2 (Step 1) + Task 3 (defaultDate). ✅
- `TxRow` DRY + filtros reutilizados → Task 2 (Steps 3-5). ✅
- #6b tela cheia "Ver todas" → Task 2 (Step 6). ✅
- z-index do modal de edição por cima → nota nos Global Constraints + ordem no Step 6. ✅

**Consistência de tipos/nomes:** `size` ("md"|"full"), props `defaultDate`/`monthLabel`, `TxRow({t,cat,onEdit,onRemove})`, `fullOpen`, `filterTabs` — usados igual entre as tasks. `catById.get(...) ?? null` casa com `cat: Category | null` do `TxRow`. ✅

**Placeholders:** nenhum "TBD/TODO"; código completo em cada passo. ✅
