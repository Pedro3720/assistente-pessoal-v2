# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — secrets never go to git
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# never commit build artifacts / archives (lição do projeto anterior: app.zip)
*.zip

# capacitor: a pasta android/ é gerada no build (GitHub Actions), não versionar
/android/

# graphify: grafo de conhecimento gerado localmente (regenerável com /graphify), não versionar
/graphify-out/

# superpowers: mockups e estado da sessão de brainstorming visual, não versionar
/.superpowers/
